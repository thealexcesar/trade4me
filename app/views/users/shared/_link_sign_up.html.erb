<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <small><%= t('devise.sessions.new_here') %></small>
  <small class="ml-8 text-right"><%= link_to t('devise.sessions.register'), new_registration_path(resource_name) %><br /></small>
<% end %>